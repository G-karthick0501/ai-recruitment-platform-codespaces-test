# ============================================
# Resume Analyzer Service - Minimal Working Requirements
# Based on actual usage analysis and working local versions
# Port: 8000
# ============================================

# Core Web Framework (Working Versions from Local)
fastapi==0.120.0
uvicorn[standard]==0.38.0
python-multipart==0.0.6

# PDF Processing (Actually Used)
PyPDF2==3.0.1
pdfplumber==0.10.0
# PyMuPDF (fitz) will be installed separately

# NLP & ML (Actually Used - Working Versions)
nltk==3.9.2
spacy==3.7.2
sentence-transformers==2.2.2
numpy>=1.24.0
scikit-learn>=1.3.0

# PDF Generation (Actually Used - Working Versions)
reportlab==4.4.4

# Utilities (Actually Used)
requests==2.31.0
python-dotenv==1.0.0

# Additional dependencies that might be needed
typing-extensions>=4.0.0

# Spacy model (install separately after pip install)
# python -m spacy download en_core_web_sm