# ============================================
# Resume Analyzer Service - TRULY Minimal Requirements
# Based on ACTUAL usage analysis - only what's really used
# Port: 8000
# ============================================

# Core Web Framework
fastapi==0.120.2
uvicorn[standard]==0.38.0
python-multipart==0.0.20

# PDF Processing (Actually Used)
PyPDF2==3.0.1
pdfplumber==0.11.7

# NLP & ML (Actually Used - TF-IDF based, not torch)
nltk==3.9.2
spacy==3.8.7
numpy==2.3.3
scikit-learn==1.7.2

# PDF Generation (Actually Used)
reportlab==4.4.4

# Utilities (Actually Used)
requests==2.32.5
python-dotenv==1.2.1

# Spacy model (install separately after pip install)
# python -m spacy download en_core_web_sm

# NOTE: We do NOT use torch, transformers, or sentence-transformers
# Our Resume Analyzer uses TF-IDF + ReportLab for CPU efficiency