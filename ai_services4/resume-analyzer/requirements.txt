# ============================================
# Resume Analyzer Service - EXACT Working Versions
# Based on local working environment analysis
# Port: 8000
# ============================================

# Core Web Framework (Exact Working Versions)
fastapi==0.120.2
uvicorn[standard]==0.38.0
python-multipart==0.0.6

# PDF Processing (Actually Used)
PyPDF2==3.0.1
pdfplumber==0.10.0
# PyMuPDF (fitz) will be installed separately

# NLP & ML (Exact Working Versions from Local Environment)
nltk==3.9.2
spacy==3.8.7
sentence-transformers==5.1.2
numpy==2.3.3
scikit-learn==1.7.2

# PDF Generation (Actually Used - Working Versions)
reportlab==4.4.4

# Utilities (Actually Used)
requests==2.31.0
python-dotenv==1.0.0

# Additional dependencies from local environment
typing-extensions>=4.0.0

# Spacy model (install separately after pip install)
# python -m spacy download en_core_web_sm